#include "menu_system/menu_system.h"
#include <esp_err.h>
#include <esp_log.h>
#include <string.h>
/*
static const char* TAG = "MENU_SYSTEM"

void menu_system_init()
{
    item_t *mainMenu = ITEM_MALLOC(sizeof(item_t));
    if (!mainMenu) {
        ESP_LOGE(TAG, "OOM allocating mainMenu");
        return ESP_ERR_NO_MEM;
    }
    if (item_init(root, "/MainMenu", NULL, 2) != ESP_OK) return;

}
*/